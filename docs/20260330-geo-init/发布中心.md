# 发布中心

## 一、模块概述

发布中心负责渠道管理和内容发布功能。

### Tab 结构

| Tab | 功能 |
|-----|------|
| 渠道管理 | 渠道展示、启用/禁用、配置编辑 |

> **说明**：发布任务通过内容中心的内容列表触发，点击「发布」按钮弹出发布浮层，无需单独的发布队列 Tab。

---

## 二、数据表设计

### 2.1 发布渠道表 (publish_channel)

```sql
CREATE TABLE `publish_channel` (
    `id`            BIGINT UNSIGNED  NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `name`          VARCHAR(64)      NOT NULL DEFAULT '' COMMENT '渠道名称',
    `code`          VARCHAR(32)      NOT NULL DEFAULT '' COMMENT '渠道编码',
    `icon`          VARCHAR(64)      DEFAULT NULL COMMENT '图标（emoji或图片路径）',
    `config`        TEXT             DEFAULT NULL COMMENT '配置信息（JSON格式）',
    `status`        TINYINT(1)       NOT NULL DEFAULT 1 COMMENT '状态：0-禁用 1-启用',
    `is_builtin`    TINYINT(1)       NOT NULL DEFAULT 0 COMMENT '是否内置：0-否 1-是',
    `create_time`   DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time`   DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_code` (`code`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='发布渠道表';
```

**status 说明：**

| 值 | 含义 |
|----|------|
| 0 | 禁用 |
| 1 | 启用 |

**is_builtin 说明：**

| 值 | 含义 |
|----|------|
| 0 | 自定义渠道 |
| 1 | 系统内置渠道 |

**预置渠道数据：**

```sql
INSERT INTO `publish_channel` (`name`, `code`, `icon`, `status`, `is_builtin`) VALUES
('微博', 'weibo', '📱', 1, 1),
('知乎', 'zhihu', '📝', 1, 1),
('微信公众号', 'wechat', '💬', 1, 1),
('小红书', 'xiaohongshu', '📕', 1, 1),
('抖音', 'douyin', '🎵', 1, 1),
('B站', 'bilibili', '📺', 1, 1);
```

**config 示例：**

```json
{
    "accountId": "公众号ID",
    "webhookUrl": "https://webhook.example.com/publish"
}
```

---

### 2.2 发布任务表 (publish_task)

```sql
CREATE TABLE `publish_task` (
    `id`            BIGINT UNSIGNED  NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `content_id`    BIGINT UNSIGNED  NOT NULL COMMENT '关联内容ID',
    `channel_id`    BIGINT UNSIGNED  NOT NULL COMMENT '发布渠道ID',
    `status`        TINYINT(1)       NOT NULL DEFAULT 0 COMMENT '状态：0-待发布 1-发布中 2-已发布 3-失败',
    `publish_url`   VARCHAR(512)     DEFAULT NULL COMMENT '发布后外链地址',
    `error_msg`     VARCHAR(256)     DEFAULT NULL COMMENT '错误信息',
    `created_by`    VARCHAR(64)      NOT NULL DEFAULT '' COMMENT '创建人',
    `create_time`   DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time`   DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
    `completed_at`  DATETIME         DEFAULT NULL COMMENT '完成时间',
    PRIMARY KEY (`id`),
    KEY `idx_content_id` (`content_id`),
    KEY `idx_channel_id` (`channel_id`),
    KEY `idx_status` (`status`),
    KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='发布任务表';
```

**status 说明：**

| 值 | 含义 |
|----|------|
| 0 | 待发布 |
| 1 | 发布中 |
| 2 | 已发布 |
| 3 | 失败 |
```

---

## 三、数据流向

```
┌─────────────────────────────────────────────────────────────────┐
│                         发布中心数据流                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────┐                                               │
│   │  内容中心    │  ◄── 点击「发布」按钮触发                      │
│   │  内容列表    │                                               │
│   └──────┬──────┘                                               │
│          │                                                      │
│          │ 弹出发布浮层                                          │
│          ▼                                                      │
│   ┌─────────────────┐                                           │
│   │   发布浮层       │  ◄── 选择单个渠道                         │
│   │  内容 + 渠道     │                                           │
│   └────────┬────────┘                                           │
│            │                                                    │
│            │ 创建发布任务                                        │
│            ▼                                                    │
│   ┌─────────────┐         ┌─────────────────┐                   │
│   │publish_task │ ──────► │ publish_channel │                   │
│   │  发布任务    │         │    渠道配置      │                   │
│   └─────────────┘         └─────────────────┘                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 四、页面设计

### 4.1 渠道管理

```
┌─────────────────────────────────────────────────────────────────┐
│  [渠道管理]                                                      │
├─────────────────────────────────────────────────────────────────┤
│  渠道列表                                                       │
│  ┌────────┬────────┬────────┬────────┬───────────────────────┐ │
│  │ 图标   │ 名称   │ 编码   │ 状态   │ 操作                  │ │
│  ├────────┼────────┼────────┼────────┼───────────────────────┤ │
│  │ 📱     │ 微博   │ weibo  │ ● 启用 │ 配置  禁用            │ │
│  │ 📝     │ 知乎   │ zhihu  │ ● 启用 │ 配置  禁用            │ │
│  │ 💬     │ 微信公众号│wechat │ ● 启用 │ 配置  禁用          │ │
│  │ 📕     │ 小红书 │ xiaohongshu│ ● 启用 │ 配置  禁用       │ │
│  │ 🎵     │ 抖音   │ douyin│ ○ 禁用 │ 配置  启用            │ │
│  │ 📺     │ B站    │ bilibili│ ○ 禁用 │ 配置  启用          │ │
│  └────────┴────────┴────────┴────────┴───────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

**配置浮层：**
```
┌─────────────────────────────────────────────┐
│  渠道配置 - 微博                       [×]  │
├─────────────────────────────────────────────┤
│  渠道名称                                   │
│  ┌───────────────────────────────────────┐ │
│  │ 微博                             🔒   │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  账号ID                                     │
│  ┌───────────────────────────────────────┐ │
│  │                                       │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  Webhook URL（可选）                         │
│  ┌───────────────────────────────────────┐ │
│  │ https://webhook.example.com/publish   │ │
│  └───────────────────────────────────────┘ │
│                                             │
│                    [取消]    [保存]         │
└─────────────────────────────────────────────┘
```

**功能说明：**
- **渠道列表**：展示所有内置渠道
- **启用/禁用**：切换渠道状态
- **配置**：编辑渠道配置信息
- **内置渠道**：系统预置，不可删除

---

### 4.2 发布浮层（从内容中心触发）

> 发布功能从内容中心的内容列表触发，点击「发布」按钮弹出浮层。

**发布浮层：**
```
┌─────────────────────────────────────────────┐
│  发布到渠道                             [×]  │
├─────────────────────────────────────────────┤
│  内容预览                                   │
│  ┌───────────────────────────────────────┐ │
│  │ 标题：北京天安门攻略指南               │ │
│  │ 来源：任务生成                         │ │
│  │ 字数：2,350                            │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  选择渠道                                   │
│  ┌───────────────────────────────────────┐ │
│  │ ○ 📱 微博                              │ │
│  │ ○ 📝 知乎                              │ │
│  │ ○ 💬 微信公众号                        │ │
│  │ ○ 📕 小红书                            │ │
│  │ ○ 🎵 抖音  （已禁用）                  │ │
│  │ ○ 📺 B站   （已禁用）                  │ │
│  └───────────────────────────────────────┘ │
│                                             │
│                    [取消]    [确认发布]     │
└─────────────────────────────────────────────┘
```

**发布中状态：**
```
┌─────────────────────────────────────────────┐
│  发布中...                             [×]  │
├─────────────────────────────────────────────┤
│                                             │
│              ⏳ 正在发布到微博...            │
│                                             │
│                                             │
└─────────────────────────────────────────────┘
```

**发布成功：**
```
┌─────────────────────────────────────────────┐
│  发布成功                              [×]  │
├─────────────────────────────────────────────┤
│  内容：北京天安门攻略指南                   │
│  渠道：📱 微博                              │
│                                             │
│  发布链接                                   │
│  ┌───────────────────────────────────────┐ │
│  │ https://weibo.com/123456       [复制] │ │
│  └───────────────────────────────────────┘ │
│                                             │
│                              [关闭]         │
└─────────────────────────────────────────────┘
```

**发布失败：**
```
┌─────────────────────────────────────────────┐
│  发布失败                              [×]  │
├─────────────────────────────────────────────┤
│  内容：北京天安门攻略指南                   │
│  渠道：📱 微博                              │
│                                             │
│  错误信息                                   │
│  ┌───────────────────────────────────────┐ │
│  │ 认证失败，请检查渠道配置               │ │
│  └───────────────────────────────────────┘ │
│                                             │
│                    [关闭]    [重试]         │
└─────────────────────────────────────────────┘
```

**功能说明：**
- **单文章单渠道**：每次发布只能选择一个渠道
- **内容预览**：展示标题、来源、字数
- **渠道选择**：单选，禁用的渠道不可选
- **实时反馈**：发布中、成功、失败三种状态

---

## 五、接口设计

### 5.1 发布任务接口

| 接口 | 方法 | 参数示例 |
|------|------|---------|
| `/api/publishTask/add` | POST | `{"contentId":1,"channelId":2}` |
| `/api/publishTask/detail` | GET | `?id=1` |
| `/api/publishTask/retry` | POST | `{"id":1}` |

### 5.2 发布渠道接口

| 接口 | 方法 | 参数示例 |
|------|------|---------|
| `/api/publishChannel/list` | GET | `?status=1` |
| `/api/publishChannel/detail` | GET | `?id=1` |
| `/api/publishChannel/update` | POST | `{"id":1,"config":{...}}` |
| `/api/publishChannel/toggleStatus` | POST | `{"id":1,"status":0}` |

---

## 六、模块关联

```
┌─────────────┐    点击发布     ┌─────────────┐
│  内容中心    │ ─────────────►│  发布浮层    │
│  内容列表    │               │  选择渠道    │
│  [发布]按钮 │               └──────┬──────┘
└─────────────┘                      │
                                     │ 确认发布
                                     ▼
                              ┌─────────────┐
                              │publish_task │
                              │  发布任务    │
                              └──────┬──────┘
                                     │
                                     │ channel_id
                                     ▼
                              ┌─────────────┐
                              │ publish_    │
                              │ channel     │
                              └─────────────┘
```

**关联说明：**
- 发布功能从内容中心触发，无独立发布队列页面
- 每次发布：单篇文章 + 单个渠道
- 发布任务通过 `content_id` 关联内容表
- 发布任务通过 `channel_id` 关联渠道表
