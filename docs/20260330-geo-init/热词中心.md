# 热词中心

## 一、模块概述

热词中心负责热词的管理、挖掘和扩词功能。

### Tab 结构

| Tab | 功能 |
|-----|------|
| 热词管理 | 热词展示、手动导入/新增、编辑、删除 |
| 热词挖掘 | 新建挖掘任务、任务列表、结果查看 |
| 热词扩词 | 新建扩词任务、任务列表、结果查看 |

---

## 二、数据表设计

### 2.1 热词表 (hot_word)

```sql
CREATE TABLE `hot_word` (
    `id`            BIGINT UNSIGNED  NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `word`          VARCHAR(256)     NOT NULL DEFAULT '' COMMENT '热词内容',
    `source_type`   TINYINT(1)       NOT NULL DEFAULT 0 COMMENT '来源类型：0-手动导入 1-热词挖掘',
    `source_task_id` BIGINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '来源任务ID（source_type=1时有效）',
    `tags`          TEXT             DEFAULT NULL COMMENT '标签（JSON数组格式）',
    `create_time`   DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time`   DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
    PRIMARY KEY (`id`),
    KEY `idx_word` (`word`),
    KEY `idx_source_type` (`source_type`),
    KEY `idx_source_task_id` (`source_task_id`),
    KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='热词表';
```

**source_type 说明：**

| 值 | 含义 | 说明 |
|----|------|------|
| 0 | 手动导入 | 手动新增/导入 |
| 1 | 热词挖掘 | 挖掘任务产生 |

**tags 示例：**
```json
["科技", "AI", "热门"]
```

---

### 2.2 热词任务表 (hot_word_task)

```sql
CREATE TABLE `hot_word_task` (
    `id`            BIGINT UNSIGNED  NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `name`          VARCHAR(128)     NOT NULL DEFAULT '' COMMENT '任务名称',
    `type`          VARCHAR(32)      NOT NULL DEFAULT '' COMMENT '任务类型：dig-热词挖掘 expand-热词扩词',
    `params`        TEXT             DEFAULT NULL COMMENT '任务参数（JSON格式字符串）',
    `status`        TINYINT(1)       NOT NULL DEFAULT 0 COMMENT '状态：0-待执行 1-执行中 2-已完成 3-失败',
    `result`        TEXT             DEFAULT NULL COMMENT '执行结果（JSON格式字符串）',
    `created_by`    VARCHAR(64)      NOT NULL DEFAULT '' COMMENT '创建人',
    `create_time`   DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time`   DATETIME         NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
    `completed_at`  DATETIME         DEFAULT NULL COMMENT '完成时间',
    PRIMARY KEY (`id`),
    KEY `idx_type` (`type`),
    KEY `idx_status` (`status`),
    KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='热词任务表';
```

**type 枚举：**

| 值 | 说明 |
|----|------|
| `dig` | 热词挖掘 |
| `expand` | 热词扩词 |

**params 示例：**

```json
// type = 'dig' 热词挖掘
{
    "keywords": ["AI", "GPT"],
    "count": 10
}

// type = 'expand' 热词扩词
{
    "hotWordIds": [1, 2, 3],
    "countPerWord": 5,
    "style": "通用问题"
}
```

**result 示例：**

```json
// type = 'dig' 挖掘结果
{
    "total": 10,
    "words": ["GPT-5", "DeepSeek", "通义千问"]
}

// type = 'expand' 扩词结果
{
    "total": 15,
    "items": [
        {
            "hotWordId": 1,
            "word": "北京天安门",
            "questions": [
                "北京天安门门票在哪买",
                "北京天安门开放时间",
                "北京天安门怎么预约"
            ]
        }
    ]
}
```

---

## 三、数据流向

```
┌─────────────────────────────────────────────────────────────────┐
│                         热词中心数据流                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌──────────┐        ┌──────────┐                             │
│   │ 手动导入 │───────►│ hot_word │                             │
│   └──────────┘        └────┬─────┘                             │
│                            │                                    │
│   ┌──────────┐            │         ┌──────────┐               │
│   │ 挖掘任务  │───────────┼────────►│ hot_word │               │
│   │ type=dig │            │         │(source=1)│               │
│   └──────────┘            │         └──────────┘               │
│                           │                                     │
│                           ▼                                     │
│                    ┌─────────────┐                              │
│                    │  扩词任务    │                              │
│                    │ type=expand │                              │
│                    └──────┬──────┘                              │
│                           │                                     │
│                           ▼                                     │
│                    ┌─────────────────┐                          │
│                    │hot_word_task    │  ◄── 结果仅存任务表      │
│                    │    .result      │     不产生新热词         │
│                    │   (问题列表)    │                          │
│                    └─────────────────┘                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 四、页面设计

### 4.1 热词管理

```
┌─────────────────────────────────────────────────────────────────┐
│  [热词管理]  [热词挖掘]  [热词扩词]              [+ 新增热词]   │
├─────────────────────────────────────────────────────────────────┤
│  来源: [全部] [手动] [挖掘]                        [手动导入]   │
├─────────────────────────────────────────────────────────────────┤
│  热词展示                                                       │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  [北京天安门] [GPT-5发布] [两会AI政策] [科技趋势]         │ │
│  │  [行业观察]   [技术突破]   [智能驾驶]   [大模型]         │ │
│  └───────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│  热词列表                                                       │
│  ┌──────────────┬────────┬──────────┬──────────┬────────┐      │
│  │ 热词         │ 标签   │ 来源     │ 任务ID   │ 操作   │      │
│  ├──────────────┼────────┼──────────┼──────────┼────────┤      │
│  │ 北京天安门   │ 旅游   │ 手动     │ -        │ 编辑 删│      │
│  │ GPT-5发布    │ 科技   │ 挖掘     │ 123      │ 编辑 删│      │
│  │ 两会AI政策   │ 政策   │ 挖掘     │ 124      │ 编辑 删│      │
│  └──────────────┴────────┴──────────┴──────────┴────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

**功能说明：**
- **热词展示**：Tags 云展示，点击可查看详情
- **来源筛选**：全部 / 手动 / 挖掘
- **新增热词**：点击弹出浮层，输入热词和标签
- **手动导入**：批量导入（支持文本粘贴，每行一个）
- **编辑/删除**：单条操作

---

### 4.2 热词挖掘

```
┌─────────────────────────────────────────────────────────────────┐
│  [热词管理]  [热词挖掘]  [热词扩词]              [+ 新建任务]   │
├─────────────────────────────────────────────────────────────────┤
│  任务列表                                                       │
│  ┌────────────────┬────────┬──────────┬──────────┬─────────┐   │
│  │ 任务名称       │ 方式   │ 状态     │ 创建时间 │ 操作    │   │
│  ├────────────────┼────────┼──────────┼──────────┼─────────┤   │
│  │ 科技热点挖掘   │ LLM    │ 已完成   │ 14:30    │ 查看    │   │
│  │ AI行业追踪     │ LLM    │ 运行中   │ 14:15    │ 查看 取消│   │
│  │ 微博热搜抓取   │ LLM    │ 失败     │ 10:00    │ 重试    │   │
│  └────────────────┴────────┴──────────┴──────────┴─────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

**新建任务浮层：**
```
┌─────────────────────────────────────────────┐
│  新建挖掘任务                          [×]  │
├─────────────────────────────────────────────┤
│  任务名称                                   │
│  ┌───────────────────────────────────────┐ │
│  │                                       │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  关键词（可选）                             │
│  ┌───────────────────────────────────────┐ │
│  │ AI, GPT, 大模型                       │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  预期数量                                   │
│  ┌───────────────────────────────────────┐ │
│  │ 10                            ▼       │ │
│  └───────────────────────────────────────┘ │
│                                             │
│                    [取消]    [开始执行]     │
└─────────────────────────────────────────────┘
```

**任务结果弹窗：**
```
┌─────────────────────────────────────────────┐
│  挖掘结果 - 科技热点挖掘              [×]   │
├─────────────────────────────────────────────┤
│  共挖掘 10 个热词                           │
│                                             │
│  ┌────────────────────────────────────────┐│
│  │ ☑ GPT-5发布                           ││
│  │ ☑ DeepSeek-V2                         ││
│  │ ☑ 通义千问新功能                       ││
│  │ ☐ AI投资趋势                          ││
│  │ ☐ 大模型降价                          ││
│  │ ...                                   ││
│  └────────────────────────────────────────┘│
│                                             │
│  全选 / 取消全选                            │
│                                             │
│                    [取消]    [导入选中]     │
└─────────────────────────────────────────────┘
```

---

### 4.3 热词扩词

```
┌─────────────────────────────────────────────────────────────────┐
│  [热词管理]  [热词挖掘]  [热词扩词]              [+ 新建任务]   │
├─────────────────────────────────────────────────────────────────┤
│  任务列表                                                       │
│  ┌────────────────┬────────┬──────────┬──────────┬─────────┐   │
│  │ 任务名称       │ 热词数 │ 状态     │ 创建时间 │ 操作    │   │
│  ├────────────────┼────────┼──────────┼──────────┼─────────┤   │
│  │ 景区扩词       │ 3      │ 已完成   │ 14:30    │ 查看    │   │
│  │ 科技话题扩词   │ 5      │ 运行中   │ 14:15    │ 查看 取消│   │
│  │ AI产品扩词     │ 10     │ 失败     │ 10:00    │ 重试    │   │
│  └────────────────┴────────┴──────────┴──────────┴─────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

**新建任务浮层：**
```
┌─────────────────────────────────────────────┐
│  新建扩词任务                          [×]  │
├─────────────────────────────────────────────┤
│  选择热词                                   │
│  ┌───────────────────────────────────────┐ │
│  │ ☑ 北京天安门                          │ │
│  │ ☑ GPT-5发布                           │ │
│  │ ☐ 智能驾驶                            │ │
│  │ ☐ 科技趋势                            │ │
│  │ ...                                   │ │
│  └───────────────────────────────────────┘ │
│  已选 2 个热词                              │
│                                             │
│  每个热词扩词数量                           │
│  ┌───────────────────────────────────────┐ │
│  │ 5                             ▼       │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  扩词风格                                   │
│  ┌───────────────────────────────────────┐ │
│  │ 通用问题                       ▼      │ │
│  └───────────────────────────────────────┘ │
│                                             │
│                    [取消]    [开始执行]     │
└─────────────────────────────────────────────┘
```

**任务结果弹窗：**
```
┌─────────────────────────────────────────────┐
│  扩词结果 - 景区扩词                  [×]   │
├─────────────────────────────────────────────┤
│  北京天安门 (5个问题)                        │
│  ┌────────────────────────────────────────┐│
│  │ • 北京天安门门票在哪买                 ││
│  │ • 北京天安门开放时间                   ││
│  │ • 北京天安门怎么预约                   ││
│  │ • 北京天安门附近有什么好吃的           ││
│  │ • 北京天安门游玩攻略                   ││
│  └────────────────────────────────────────┘│
│                                             │
│  GPT-5发布 (5个问题)                        │
│  ┌────────────────────────────────────────┐│
│  │ • GPT-5什么时候发布                    ││
│  │ • GPT-5有哪些新功能                    ││
│  │ • ...                                  ││
│  └────────────────────────────────────────┘│
│                                             │
│                              [关闭]         │
└─────────────────────────────────────────────┘
```

---

## 五、接口设计

### 5.1 热词接口

| 接口 | 方法 | 参数示例 |
|------|------|---------|
| `/api/hotWord/list` | GET | `?sourceType=0&page=1&size=20` |
| `/api/hotWord/add` | POST | `{"word":"北京天安门","tags":["旅游"]}` |
| `/api/hotWord/import` | POST | `{"words":["北京天安门","GPT-5发布"]}` |
| `/api/hotWord/update` | POST | `{"id":1,"word":"北京天安门","tags":["旅游"]}` |
| `/api/hotWord/delete` | POST | `{"id":1}` |

### 5.2 热词任务接口

| 接口 | 方法 | 参数示例 |
|------|------|---------|
| `/api/hotWordTask/list` | GET | `?type=dig&page=1&size=20` |
| `/api/hotWordTask/add` | POST | `{"name":"科技挖掘","type":"dig","params":{...}}` |
| `/api/hotWordTask/detail` | GET | `?id=1` |
| `/api/hotWordTask/cancel` | POST | `{"id":1}` |
| `/api/hotWordTask/retry` | POST | `{"id":1}` |
